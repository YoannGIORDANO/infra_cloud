## La configuration force Wiki.js à rester derrière Nginx et à joindre PostgreSQL.
port: 3000
bindIP: 127.0.0.1

db:
  type: postgres
  host: {{ db_host }}
  port: 5432
  user: {{ db_user }}
  pass: "{{ db_password }}"
  db: {{ db_name }}
  ssl: false

logLevel: info
